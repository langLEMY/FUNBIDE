VITE_SUPABASE_URL=https://CHANGE_ME.supabase.co
VITE_SUPABASE_ANON_KEY=CHANGE_ME
VITE_API_BASE_URL=https://api.funbide.org

# VITE_AUTH_MODE=local activa la instalación offline/USB (sin Supabase): el login pasa
# por el backend (POST /api/auth/login) en vez de ir directo a la nube. Dejar sin
# definir (o en cualquier otro valor) para el modo normal con Supabase.
# VITE_AUTH_MODE=local
